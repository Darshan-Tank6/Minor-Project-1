<!-- <!DOCTYPE html>
<html>
  <head>
    <title>Create Department</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 p-8">
    <div class="max-w-lg mx-auto bg-white p-6 rounded-2xl shadow">
      <h2 class="text-2xl font-bold mb-4">
        Create Department for <%= college.name %>
      </h2>

      <form
        method="POST"
        action="/admin/department/create/<%= college._id %>"
        class="space-y-4"
      >
        <input
          type="text"
          name="name"
          placeholder="Department Name"
          required
          class="w-full p-2 border rounded"
        />
        <button
          type="submit"
          class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700"
        >
          Create Department
        </button>
      </form>

      <a
        href="/admin/colleges"
        class="inline-block mt-4 text-gray-600 hover:underline"
        >← Back to Colleges</a
      >
    </div>
  </body>
</html> -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Department</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gradient-to-r from-purple-50 to-purple-100 min-h-screen">
    <!-- Navbar -->
    <nav
      class="bg-gradient-to-r from-purple-700 to-purple-900 text-white shadow-md"
    >
      <div
        class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center"
      >
        <!-- Logo + Title -->
        <div class="flex items-center space-x-3">
          <img
            src="/images/logo.jpg"
            alt="EduBridge Logo"
            class="h-10 w-auto rounded"
          />
          <h1 class="text-xl font-bold">EduBridge</h1>
        </div>
        <!-- Navbar Links -->
        <ul class="flex space-x-6">
          <li><a href="/" class="hover:text-gray-200">Home</a></li>
          <li><a href="/colleges" class="hover:text-gray-200">Colleges</a></li>
          <li>
            <a href="/departments" class="hover:text-gray-200">Departments</a>
          </li>
          <li><a href="/classes" class="hover:text-gray-200">Classes</a></li>
          <li><a href="/auth/logout" class="hover:text-gray-200">Logout</a></li>
        </ul>
      </div>
    </nav>
    <!-- Main Section -->
    <div class="flex items-center justify-center p-6">
      <div
        class="max-w-lg w-full bg-white p-8 mt-10 rounded-2xl shadow-xl border border-gray-200"
      >
        <h2 class="text-3xl font-bold text-purple-700 mb-6 text-center">
          Create Department for <%= college.name %>
        </h2>
        <!-- Form -->
        <form
          method="POST"
          action="/admin/department/create/<%= college._id %>"
          class="space-y-5"
        >
          <!-- Department Name -->
          <div>
            <label class="block text-gray-700 font-medium mb-2"
              >Department Name</label
            >
            <input
              type="text"
              name="name"
              placeholder="e.g. Computer Science"
              required
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400 focus:outline-none"
            />
          </div>
          <!-- Submit -->
          <button
            type="submit"
            class="w-full bg-purple-700 text-white py-3 rounded-lg font-semibold hover:bg-purple-800 transition-all"
          >
            Create Department
          </button>
        </form>
        <a
          href="/admin/colleges"
          class="block mt-6 text-purple-700 font-medium text-center hover:underline"
        >
          ← Back to Colleges
        </a>
      </div>
    </div>
  </body>
</html>
