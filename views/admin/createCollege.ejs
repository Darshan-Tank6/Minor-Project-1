<!DOCTYPE html>
<html>
  <head>
    <title>Create College</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 p-8">
    <% if (success_msg && success_msg.length > 0) { %>
    <div class="custom-alert-success"><%= success_msg %></div>
    <% } %> <% if(error_msg && error_msg.length >0) { %>
    <div class="custom-alert"><%= error_msg %></div>
    <%}%> <% if(error && error.length > 0) { %>
    <div class="custom-alert"><%= error_msg %></div>
    <% }%>
    <div class="max-w-xl mx-auto bg-white p-6 rounded-2xl shadow">
      <h2 class="text-2xl font-bold mb-6">Create College</h2>

      <form method="POST" action="/admin/college" class="space-y-4">
        <input
          type="text"
          name="name"
          placeholder="College Name"
          required
          class="w-full p-2 border rounded"
        />
        <input
          type="text"
          name="location"
          placeholder="Location"
          class="w-full p-2 border rounded"
        />
        <button
          type="submit"
          class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700"
        >
          Create
        </button>
      </form>
    </div>
  </body>
</html>
