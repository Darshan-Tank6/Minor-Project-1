<!-- <!DOCTYPE html>
<html>
  <head>
    <title>Create Class</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 p-8">
    <div class="max-w-lg mx-auto bg-white p-6 rounded-2xl shadow">
      <h2 class="text-2xl font-bold mb-4">
        Create Class in <%= department.name %> (<%= department.collegeId.name
        %>)
      </h2>

      <form
        method="POST"
        action="/admin/class/create/<%= department._id %>"
        class="space-y-4"
      >
        <input
          type="text"
          name="name"
          placeholder="Class Name (e.g. BSc CS A)"
          required
          class="w-full p-2 border rounded"
        />
        <input
          type="number"
          name="year"
          placeholder="Year (e.g. 1, 2, 3)"
          required
          class="w-full p-2 border rounded"
        />
        <button
          type="submit"
          class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700"
        >
          Create Class
        </button>
      </form>

      <a
        href="/admin/college/<%= department.collegeId._id %>/departments"
        class="inline-block mt-4 text-gray-600 hover:underline"
        >← Back to Departments</a
      >
    </div>
  </body>
</html> -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Class</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gradient-to-r from-purple-50 to-purple-100 min-h-screen">
    <!-- Navbar -->
    <nav class="bg-purple-700 text-white shadow-md">
      <div
        class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center"
      >
        <!-- Logo + Title -->
        <div class="flex items-center space-x-3">
          <img
            src="/images/logo.jpg"
            alt="EduBridge Logo"
            class="h-10 w-auto filter invert brightness-0"
          />
          <h1 class="text-xl font-bold">EduBridge</h1>
        </div>
        <!-- Navbar Links -->
        <ul class="flex space-x-6">
          <li><a href="/" class="hover:text-gray-200">Home</a></li>
          <li>
            <a href="/departments" class="hover:text-gray-200">Departments</a>
          </li>
          <li><a href="/classes" class="hover:text-gray-200">Classes</a></li>
          <li><a href="/auth/logout" class="hover:text-gray-200">Logout</a></li>
        </ul>
      </div>
    </nav>
    <!-- Main Section -->
    <div class="flex items-center justify-center p-6">
      <div
        class="max-w-lg w-full bg-white p-8 mt-10 rounded-2xl shadow-xl border border-gray-200"
      >
        <h2 class="text-3xl font-bold text-purple-700 mb-6 text-center">
          Create Class in <%= department.name || "Computer Science" %> (<%=
          department.collegeId.name || "ABC College" %>)
        </h2>
        <form method="POST" action="/classes/create" class="space-y-5">
          <!-- Class Name -->
          <div>
            <label class="block text-gray-700 font-medium mb-2"
              >Class Name</label
            >
            <input
              type="text"
              name="name"
              placeholder="e.g. BSc CS A"
              required
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400 focus:outline-none"
            />
          </div>
          <!-- Year -->
          <div>
            <label class="block text-gray-700 font-medium mb-2">Year</label>
            <input
              type="number"
              name="year"
              placeholder="e.g. 1, 2, 3"
              required
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400 focus:outline-none"
            />
          </div>
          <!-- Submit -->
          <button
            type="submit"
            class="w-full bg-purple-600 text-white py-3 rounded-lg font-semibold hover:bg-purple-700 transition-all"
          >
            Create Class
          </button>
        </form>
        <a
          href="/admin/college/<%= department.collegeId._id %>/departments"
          class="block mt-6 text-purple-600 font-medium text-center hover:underline"
        >
          ← Back to Departments
        </a>
      </div>
    </div>
  </body>
</html>
