<!DOCTYPE html>
<html>
  <head>
    <title>Create Subject</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100 p-8">
    <div class="max-w-lg mx-auto bg-white p-6 rounded-2xl shadow">
      <h2 class="text-2xl font-bold mb-4">
        Create Subject in <%= department.name %> (<%= department.college.name
        %>)
      </h2>

      <form
        method="POST"
        action="/admin/subject/create/<%= department._id %>"
        class="space-y-4"
      >
        <input
          type="text"
          name="name"
          placeholder="Subject Name (e.g. Data Structures)"
          required
          class="w-full p-2 border rounded"
        />
        <input
          type="text"
          name="code"
          placeholder="Subject Code (e.g. CS201)"
          class="w-full p-2 border rounded"
        />
        <button
          type="submit"
          class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700"
        >
          Create Subject
        </button>
      </form>

      <a
        href="/admin/college/<%= department.college._id %>/departments"
        class="inline-block mt-4 text-gray-600 hover:underline"
        >← Back to Departments</a
      >
    </div>
  </body>
</html>
